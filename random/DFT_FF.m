//replacing standard duality with Fourier transform (CRT decomposition)
//F_q^n <-> F_q[x]/phi_m(x), n=Eulerphi(m) <-> if q = 1 mod m, the ring splits into linear factors (not really needed? just have bigger chunks) that can be seen as F_q^n again

//computes the analogue of Krawchuk matrices for the case q = 1 mod m
function duality_matr(n,q)

end function;